#!/usr/bin/python3
# pyside2不再单独学习，融合到Qt中学习，UI用Qt创建并转换即可
# pyside2 与 pyqt5 完全兼容，可以直接替换导入包替换执行

import os

# opensuse库安装python3-pyside2
os.system("sudo zypper in python3-pyside2")

# pip方式安装pyside2
os.system("sudo pip install pyside2")
